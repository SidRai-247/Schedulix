#include <bits/stdc++.h>
#define ll long long
#define all(x) (x).begin(), (x).end()
#define pb push_back
#define ff first
#define ss second
#define rep(i, n) for (ll i = 0; i < (n); ++i)
#define yes cout << "YES\n"
#define no cout << "NO\n"
using namespace std;

void solve()
{
    ll n;
    cin >> n;
    vector<ll> arr(n);
    rep(i, n) {
        cin >> arr[i];
    }
    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    ll t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}